import { ImageUploadPipePipe } from './image.upload.pipe.pipe';

describe('ImageUploadPipePipe', () => {
  it('should be defined', () => {
    expect(new ImageUploadPipePipe()).toBeDefined();
  });
});
